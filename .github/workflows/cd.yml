name: CD

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: cd-production
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  quality:
    name: Quality gate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint backend
        run: npx eslint "{src,test}/**/*.ts" --max-warnings 0
        working-directory: backend

      - name: Type check backend
        run: npx tsc --noEmit
        working-directory: backend

      - name: Test backend
        run: npm test -- --ci
        working-directory: backend

      - name: Build backend
        run: npm run build
        working-directory: backend

      - name: Lint frontend
        run: npm run lint --workspace=frontend

      - name: Type check frontend
        run: npm exec --workspace=frontend -- tsc --noEmit

      - name: Build frontend
        run: npm run build --workspace=frontend
        env:
          NEXT_TELEMETRY_DISABLED: "1"

  deploy-production:
    name: Deploy to production
    needs: quality
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install SST platform
        run: ./node_modules/.bin/sst install

      - name: Build backend
        run: npm run build --workspace=backend

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: us-east-1

      - name: Sync SST secrets (production)
        env:
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          SES_SENDER_EMAIL: ${{ secrets.SES_SENDER_EMAIL }}
          SEED_ADMIN_EMAIL: ${{ secrets.SEED_ADMIN_EMAIL }}
          SEED_ADMIN_PASSWORD: ${{ secrets.SEED_ADMIN_PASSWORD }}
          SEED_ADMIN_FIRST_NAME: ${{ secrets.SEED_ADMIN_FIRST_NAME }}
          SEED_ADMIN_LAST_NAME: ${{ secrets.SEED_ADMIN_LAST_NAME }}
        run: bash scripts/sync-sst-secrets.sh production

      - name: Deploy SST (production)
        run: ./node_modules/.bin/sst deploy --stage production
